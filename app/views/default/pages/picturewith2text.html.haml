.booyah-box
	.booyah-box
		.container-fluid
			.row
				%img{:class => "uploaded-pictures", :src => @page.content(:photo).file_url}
				%br/
		.page-title
			%h1= @page.title
		%br/
	.booyah-box
		.row.container-fluid
			.col-sm-4.text-content.double-text
				= @page.content(:text).try(:html_safe)
			.col-sm-8.text-content
				=@page.content(:text2).try(:html_safe)

